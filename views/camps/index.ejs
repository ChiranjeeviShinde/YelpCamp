<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Camps</title>
</head>

<body>
    <ul>
        <% for(let camp of camps) { %>
        <!-- <li><a href="/camps/<%=camp._id%>"><%= camp.name %></a> </li> -->
         <li><a href="/camps/<%=camp._id%>"><%= JSON.stringify(camp) %></a> </li>
        <% }%>
    </ul>
    <a href="/camps/new">New Camp</a>
</body>

</html>